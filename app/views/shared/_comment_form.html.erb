<%= simple_form_for([article, comment], remote: true) do |f| %>
<%= f.error_notification %>
<%= f.input :title, as: :text, placeholder: 'Title' %>
<%= f.input :content, as: :text, placeholder: 'Comment' %>
<%= f.button :submit %>
<%= f.hidden_field :reading_id, value: @reading.id %>
<% end %>


